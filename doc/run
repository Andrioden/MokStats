1. Check https://cloud.google.com/python/django/appengine
2. Make sure your local mokstats/settings.py SQL config is not pointing to PROD proxy (proxy must be started to work anyway)
3. run_dev_server.ps1 is a helper script that can be used when everything is set up correctly