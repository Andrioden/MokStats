HIGH PRI
------------------
- Can register new matches through mobile, idea: auth using a predined password, to avoid having to login to django.

LOW PRI
------------------
- Filter player stats by period/year.
- Ha en liste som viser hvilke spillere har best prestasjon for alle rundene, i en liste form.

PERFORMANCE / INTERNAL STUFF
----------------------------
- Can ALLOWED_HOSTS = ["127.0.0.1", "localhost"] be used to set debug=false? Investigate on prod server, not localy.
- Add field to PlayerResult: solitaire_sum, summarize the solitaire values "on save" to simplify everything in regard to it.
- Add field to PlayerResult: rating earned, can then check best earned rating and simplify rating earned calculations.